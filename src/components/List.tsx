import React from 'react'
import { IState as IProps } from '../App'

const List: React.FC<IProps> = ({students}) => {
    return (
        <div>
            list
        </div>
    )
}

export default List
